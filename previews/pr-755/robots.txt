# Prevent all web crawlers from indexing PR previews.
User-agent: *
Disallow: /previews/

# Robots.txt is typically generated by Jekyll, but since we manually created it in order
# to ignore PR previews, we also need to manually include a link to the sitemap.xml.
Sitemap: https://blog.danskingdom.com/sitemap.xml
